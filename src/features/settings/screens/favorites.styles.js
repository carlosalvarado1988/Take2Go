import styled from "styled-components/native";
import { SafeAreaViewContainer } from "../../../components/utilities/safe-area.component";

export const NoFavoritesArea = styled(SafeAreaViewContainer)`
  align-items: center;
  justify-content: center;
`;
